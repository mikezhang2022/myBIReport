# ProductionProcessBi Web

This is the standalone frontend. It is intentionally not served by the ASP.NET API project.

For local development, start a static-file server in this folder and open the displayed URL. The default API address is `http://127.0.0.1:5095`; set `window.PROCESS_BI_API_BASE` before loading `site.js` to use a different API deployment.
